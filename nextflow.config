process {
    conda.enabled = true
    conda.useMamba = true  // Optional: uses mamba for faster solving
}

// Optional: specify conda base installation
conda {
    cacheDir = "$HOME/conda-cache-nextflow"
    createTimeout = '1 h'
}